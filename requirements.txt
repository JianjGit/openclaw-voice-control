# Core runtime dependencies. For development use: pip install -e ".[dev]"
funasr>=1.2
numpy>=1.26
openwakeword>=0.6.0
PyYAML>=6.0
sounddevice>=0.5
websockets>=15.0
torch>=2.2
torchaudio>=2.2
pywin32>=306; sys_platform == "win32"

# Optional routes are exposed as pyproject extras:
#   pip install -e ".[porcupine]"
#   pip install -e ".[tts-cli]"
